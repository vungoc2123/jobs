import 'package:easy_localization/easy_localization.dart';

import '../../../gen/assets.gen.dart';

class Sector{
  String title;
  String img;
  bool isSelected;

  Sector({required this.title,required this.img,required this.isSelected});
}
